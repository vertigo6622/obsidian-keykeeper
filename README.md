# keykeeper
an online key provisioning and license management framework. designed to use a local tor daemon and proxy to route and serve the backend as a hidden service while still using clearnet domains.
